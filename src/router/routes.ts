export enum ROUTES {
    login = "/login",
    signup = "/signup",
    chat = "/chat",
    settings = "/settings",
    editSettings = "/settings/edit",
    password = "/settings/password",
    page404 = "/page404",
    page500 = "/page500",
}
